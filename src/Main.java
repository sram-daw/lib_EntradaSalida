public class Main {
    public static void main(String[] args) {

        boolean resultado = false;

        resultado = EntradaSalida.salida("Hola", EntradaSalida.SALIDA_CONSOLA);
        resultado = EntradaSalida.salida("Hello world", EntradaSalida.SALIDA_WINDOW);

    }
}